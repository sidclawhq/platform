# Task: P0.5 — Dashboard Scaffold

## Context

You are working on the **Agent Identity & Approval Layer** project. Read these files first:

1. `research/2026-03-20-product-development-plan.md` — read the **Overview**, **P0.5**, and **CC.6 (Code Style)** sections.
2. `packages/shared/src/` — shared types (created in P0.2). The dashboard must be able to import from `@sidclaw/shared`.
3. `v0-prototype/components/layout/` — reference for the layout shell design (TopNav, AppShell). Do NOT copy code directly — reimagine for the production dashboard.
4. `v0-prototype/app/globals.css` — reference for design token values.

P0.1 (monorepo) and P0.2 (shared types) are complete. The `apps/dashboard/` directory exists as a fresh Next.js 15 project (created in P0.1). Your job is to configure it with the correct design system and create the layout shell.

**Important:** This task does NOT depend on P0.3 (database) or P0.4 (API). The API may not be running yet. The API client should be created but should handle connection failures gracefully (log a warning, don't crash).

## What To Do

### 1. Install Dependencies

In `apps/dashboard/`:

```bash
npx shadcn@latest init  # select dark theme, neutral color, src/ directory
npm install sonner lucide-react
```

shadcn/ui init will configure Tailwind and create the components directory. After init, you'll customize the theme.

### 2. Design Tokens — "Institutional Calm"

This is the visual identity of the product. Every component, every page must use these tokens. Configure them in Tailwind CSS.

**Exact token values:**

```
Background primary:    #0A0A0B     → CSS var: --bg-primary     → Tailwind: bg-surface-0
Background secondary:  #111113     → CSS var: --bg-secondary   → Tailwind: bg-surface-1
Background tertiary:   #1A1A1D     → CSS var: --bg-tertiary    → Tailwind: bg-surface-2
Border default:        #2A2A2E     → CSS var: --border-default → Tailwind: border-default
Border subtle:         rgba(255, 255, 255, 0.05)  → Tailwind: border-subtle
Border emphasis:       rgba(255, 255, 255, 0.10)  → Tailwind: border-emphasis
Text primary:          #E4E4E7     → CSS var: --text-primary   → Tailwind: text-primary
Text secondary:        #A1A1AA     → CSS var: --text-secondary → Tailwind: text-secondary
Text muted:            #71717A     → CSS var: --text-muted     → Tailwind: text-muted
Accent amber:          #F59E0B     → Tailwind: text-accent-amber, bg-accent-amber
Accent green:          #22C55E     → Tailwind: text-accent-green, bg-accent-green (use at 80% opacity for buttons)
Accent red:            #EF4444     → Tailwind: text-accent-red, bg-accent-red (use at 80% opacity for buttons)
Accent blue:           #3B82F6     → Tailwind: text-accent-blue, bg-accent-blue
```

**Implementation approach:** Define CSS custom properties in `globals.css`, reference them in the Tailwind config. This allows shadcn/ui components to use the same design system.

In `src/app/globals.css` (after shadcn/ui setup):

```css
@layer base {
  :root {
    /* Surface hierarchy */
    --surface-0: 0 0% 4%;        /* #0A0A0B - hsl for shadcn compatibility */
    --surface-1: 240 5% 7%;      /* #111113 */
    --surface-2: 240 4% 10%;     /* #1A1A1D */

    /* Borders */
    --border-default: 240 3% 17%;  /* #2A2A2E */

    /* Text */
    --text-primary: 240 5% 90%;    /* #E4E4E7 */
    --text-secondary: 240 4% 66%;  /* #A1A1AA */
    --text-muted: 240 4% 46%;     /* #71717A */

    /* Accents */
    --accent-amber: 38 92% 50%;    /* #F59E0B */
    --accent-green: 142 71% 45%;   /* #22C55E */
    --accent-red: 0 84% 60%;       /* #EF4444 */
    --accent-blue: 217 91% 60%;    /* #3B82F6 */
  }
}
```

**Note:** Adjust the HSL values to match the exact hex codes listed above. The HSL values shown are approximations — verify and correct them. shadcn/ui uses HSL format without the `hsl()` wrapper (just space-separated values).

Also override the shadcn/ui CSS variables to use your tokens:

```css
@layer base {
  .dark {
    --background: var(--surface-0);
    --foreground: var(--text-primary);
    --card: var(--surface-1);
    --card-foreground: var(--text-primary);
    --popover: var(--surface-1);
    --popover-foreground: var(--text-primary);
    --primary: var(--text-primary);
    --primary-foreground: var(--surface-0);
    --secondary: var(--surface-2);
    --secondary-foreground: var(--text-primary);
    --muted: var(--surface-2);
    --muted-foreground: var(--text-muted);
    --accent: var(--surface-2);
    --accent-foreground: var(--text-primary);
    --destructive: var(--accent-red);
    --destructive-foreground: var(--text-primary);
    --border: var(--border-default);
    --input: var(--border-default);
    --ring: var(--accent-blue);
  }
}
```

Extend `tailwind.config.ts` (or the Tailwind config that shadcn/ui created) with custom utility classes:

```typescript
// Extend the theme with custom colors:
{
  colors: {
    'surface-0': 'hsl(var(--surface-0))',
    'surface-1': 'hsl(var(--surface-1))',
    'surface-2': 'hsl(var(--surface-2))',
    'accent-amber': 'hsl(var(--accent-amber))',
    'accent-green': 'hsl(var(--accent-green))',
    'accent-red': 'hsl(var(--accent-red))',
    'accent-blue': 'hsl(var(--accent-blue))',
  }
}
```

### 3. Fonts

In `src/app/layout.tsx`, load fonts via `next/font/google`:

```typescript
import { Inter, JetBrains_Mono } from 'next/font/google';

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
});

const jetbrainsMono = JetBrains_Mono({
  subsets: ['latin'],
  variable: '--font-mono',
});
```

Apply to `<html>`:

```tsx
<html lang="en" className={`dark ${inter.variable} ${jetbrainsMono.variable}`}>
  <body className="font-sans antialiased">
    {children}
  </body>
</html>
```

Configure in Tailwind:

```typescript
{
  fontFamily: {
    sans: ['var(--font-inter)', 'system-ui', 'sans-serif'],
    mono: ['var(--font-mono)', 'monospace'],
  }
}
```

### 4. Dashboard Layout Shell

Create the layout for all `/dashboard/*` pages.

**Files to create:**

```
apps/dashboard/src/
  app/
    layout.tsx                    # Root layout (html, fonts, dark mode)
    page.tsx                      # Root redirect to /dashboard
    dashboard/
      layout.tsx                  # Dashboard layout (sidebar + content)
      page.tsx                    # Dashboard overview placeholder
  components/
    layout/
      DashboardSidebar.tsx        # Left sidebar navigation
      DashboardHeader.tsx         # Top header bar (search placeholder, user placeholder)
  lib/
    api-client.ts                 # Typed HTTP client for the API
    utils.ts                      # cn() utility (shadcn/ui pattern)
```

**Root `page.tsx`** — redirect to `/dashboard`:

```typescript
import { redirect } from 'next/navigation';
export default function Home() {
  redirect('/dashboard');
}
```

**Dashboard layout (`dashboard/layout.tsx`):**

```tsx
// Sidebar on the left (240px fixed width)
// Header bar at the top of the content area
// Content area fills remaining space
// Toast container (sonner) at bottom-right

<div className="flex h-screen bg-surface-0">
  <DashboardSidebar />
  <div className="flex flex-1 flex-col overflow-hidden">
    <DashboardHeader />
    <main className="flex-1 overflow-y-auto p-6">
      {children}
    </main>
  </div>
  <Toaster position="bottom-right" theme="dark" toastOptions={{
    style: {
      background: 'hsl(var(--surface-1))',
      color: 'hsl(var(--text-primary))',
      border: '1px solid hsl(var(--border-default))',
      fontSize: '13px',
    },
  }} />
</div>
```

**DashboardSidebar:**

```tsx
// 240px wide, full height, bg-surface-1, border-r border-default
// Top section: product name "Agent Identity" in text-[13px] font-semibold
// Navigation items (vertical list):

const navItems = [
  { label: 'Overview', href: '/dashboard', icon: LayoutDashboard },
  { label: 'Agents', href: '/dashboard/agents', icon: Bot },
  { label: 'Policies', href: '/dashboard/policies', icon: Shield },
  { label: 'Approvals', href: '/dashboard/approvals', icon: CheckCircle },
  { label: 'Audit', href: '/dashboard/audit', icon: ScrollText },
];

// Use lucide-react icons (16px)
// Active item: bg-surface-2 text-primary
// Inactive item: text-secondary hover:bg-surface-2/50 hover:text-primary
// Use usePathname() for active state
// Bottom section: "Settings" link (icon: Settings) → /dashboard/settings

// Font size: 13px for nav items
// Spacing: px-3 py-2 for items, gap-0.5 between items
// Icon + label gap: gap-3
```

**DashboardHeader:**

```tsx
// Full width of content area, h-14, bg-surface-0, border-b border-default
// Left: breadcrumb placeholder (just show "Overview" or current page name for now)
// Right: placeholder for user avatar (gray circle 32px with initials) and search bar
// Search bar: text input with placeholder "Search...", bg-surface-1, border-default, text-[13px], w-64
// Search is non-functional — just a styled input. Functionality comes in P2.5.
```

**Dashboard overview page (`dashboard/page.tsx`):**

```tsx
// Simple page that confirms the shell works:
// Heading: "Dashboard" in text-lg font-medium text-primary
// Subheading: "Agent Identity & Approval Layer" in text-sm text-secondary
// A card (bg-surface-1, border border-default, rounded-lg, p-6) containing:
//   "Dashboard shell is running" in text-sm text-muted
//   API connection status: try to fetch /health from the API, display result
//   If API is unreachable: "API not connected" in text-accent-amber (this is expected if P0.4 hasn't run yet)
//   If API is healthy: "API connected — v0.1.0" in text-accent-green
```

### 5. API Client (`lib/api-client.ts`)

Create a typed HTTP client for calling the API service:

```typescript
const API_URL = process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:4000';
const isDev = API_URL.includes('localhost') || API_URL.includes('127.0.0.1');

interface RequestOptions {
  method?: 'GET' | 'POST' | 'PATCH' | 'DELETE';
  body?: unknown;
  headers?: Record<string, string>;
}

export class ApiClient {
  private baseUrl: string;

  constructor(baseUrl: string = API_URL) {
    this.baseUrl = baseUrl;
  }

  async request<T>(path: string, options: RequestOptions = {}): Promise<T> {
    const { method = 'GET', body, headers = {} } = options;

    const requestHeaders: Record<string, string> = {
      'Content-Type': 'application/json',
      ...headers,
    };

    // Dev bypass header for local development
    // TODO(P3.4): Replace with session-based auth
    if (isDev) {
      requestHeaders['X-Dev-Bypass'] = 'true';
    }

    const response = await fetch(`${this.baseUrl}${path}`, {
      method,
      headers: requestHeaders,
      body: body ? JSON.stringify(body) : undefined,
    });

    if (!response.ok) {
      const error = await response.json().catch(() => ({
        error: 'unknown',
        message: `HTTP ${response.status}`,
        status: response.status,
        request_id: response.headers.get('x-request-id') ?? 'unknown',
      }));
      throw new ApiError(error);
    }

    return response.json() as Promise<T>;
  }

  // Convenience methods
  async get<T>(path: string): Promise<T> {
    return this.request<T>(path);
  }

  async post<T>(path: string, body: unknown): Promise<T> {
    return this.request<T>(path, { method: 'POST', body });
  }

  async patch<T>(path: string, body: unknown): Promise<T> {
    return this.request<T>(path, { method: 'PATCH', body });
  }

  async delete<T>(path: string): Promise<T> {
    return this.request<T>(path, { method: 'DELETE' });
  }

  // Health check — used by the dashboard overview to show API status
  async healthCheck(): Promise<{ status: string; version: string } | null> {
    try {
      return await this.get<{ status: string; version: string }>('/health');
    } catch {
      return null;
    }
  }
}

export class ApiError extends Error {
  public readonly code: string;
  public readonly status: number;
  public readonly requestId: string;
  public readonly details?: Record<string, unknown>;

  constructor(error: { error: string; message: string; status: number; request_id: string; details?: Record<string, unknown> }) {
    super(error.message);
    this.name = 'ApiError';
    this.code = error.error;
    this.status = error.status;
    this.requestId = error.request_id;
    this.details = error.details;
  }
}

// Singleton instance
export const api = new ApiClient();
```

### 6. Environment File

Create `apps/dashboard/.env.local`:

```
NEXT_PUBLIC_API_URL=http://localhost:4000
```

### 7. Design Principles (for all future dashboard work)

Add a comment block at the top of `globals.css` documenting the design system:

```css
/*
 * Agent Identity — "Institutional Calm" Design System
 *
 * Visual identity: dark, restrained, institutional. Designed to feel like
 * enterprise security infrastructure, not consumer SaaS.
 *
 * Rules:
 * - No gradients, no decorative elements, no AI sparkle icons
 * - Borders are subtle (border-default or border-subtle)
 * - Typography is restrained: 13px for nav/labels, 14px for body, 12px for metadata
 * - Color is used sparingly and purposefully:
 *   - Amber (#F59E0B): flagged items, warnings, approval-required indicators
 *   - Green (#22C55E): approved, allowed, success states
 *   - Red (#EF4444): denied, blocked, destructive actions
 *   - Blue (#3B82F6): informational, policy evaluation, neutral badges
 * - Monospace font (JetBrains Mono) for: trace IDs, timestamps, policy versions, technical data
 * - Inter for everything else
 * - Maximum content width: 1400px (if not using sidebar layout)
 */
```

## Acceptance Criteria

- [ ] `npm run dev` in `apps/dashboard/` starts Next.js on port 3000
- [ ] `http://localhost:3000` redirects to `/dashboard`
- [ ] `/dashboard` renders with correct dark-mode styling:
  - Background is `#0A0A0B` (not Tailwind's default dark)
  - Text is `#E4E4E7`
  - No white flash on load
- [ ] Sidebar navigation renders with all 5 items + Settings
- [ ] Active nav item is visually distinguished (bg-surface-2)
- [ ] Clicking nav items navigates correctly (pages can be empty/404 — that's fine)
- [ ] Inter font loads as default body font
- [ ] JetBrains Mono is available via `font-mono` class
- [ ] Toast container is mounted (test with a manual `toast('test')` call)
- [ ] API health check on overview page:
  - Shows "API not connected" in amber if API isn't running (expected)
  - Shows "API connected" in green if API is running (after P0.4)
- [ ] Header shows search placeholder and user avatar placeholder
- [ ] Design tokens are accessible via Tailwind utilities (`bg-surface-0`, `text-accent-amber`, etc.)
- [ ] `import { Agent } from '@sidclaw/shared'` works from dashboard code
- [ ] No hydration errors in console
- [ ] `turbo build` succeeds across all packages

## Constraints

- Do NOT implement any business logic or data fetching beyond the health check
- Do NOT create agent, policy, approval, or audit pages (those are Phase 1-2)
- Do NOT implement authentication or session management (that's P3.4)
- Do NOT implement global search functionality (that's P2.5)
- Do NOT copy v0 prototype code — reference it for design direction only
- shadcn/ui components (Button, Card, Input, etc.) should work with the custom theme
- Follow code style: files in `kebab-case.tsx`, components in `PascalCase`, strict TypeScript
- The dashboard must work even if the API is not running (graceful degradation)

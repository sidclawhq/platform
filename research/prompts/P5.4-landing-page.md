# Task: P5.4 — Landing Page

## Context

You are working on the **Agent Identity & Approval Layer** project (brand: **SidClaw**, domain: `sidclaw.com`). Read these files first:

1. `research/2026-03-20-product-development-plan.md` — read **P5.4** section.
2. `research/2026-03-20-market-viability-assessment.md` — market stats for the Problem section.
3. `apps/dashboard/src/app/globals.css` — design tokens for "Institutional Calm" aesthetic.
4. `v0-prototype/app/landing/page.tsx` — reference for inspiration (if it exists).

Your job is to build the public-facing landing page at `apps/landing/`.

## What To Do

### 1. Initialize Landing Page App

```bash
cd apps
npx create-next-app landing --typescript --tailwind --app --no-src-dir
```

Configure with the same "Institutional Calm" design tokens as the dashboard.

### 2. Page Structure (Top to Bottom)

The landing page is a single long-scroll page with these sections:

#### Section 1: Hero

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│              The approval and accountability                     │
│              layer for agentic AI                                │
│                                                                  │
│   Your agents need identity, policy, and human oversight.        │
│   Not another IAM — the governance layer that's missing.         │
│                                                                  │
│   [Get Started Free]    [View on GitHub]                         │
│                                                                  │
│   npm install @sidclaw/sdk                                       │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

- Headline: `text-4xl md:text-5xl font-semibold text-primary tracking-tight`
- Subheadline: `text-lg text-secondary max-w-2xl`
- CTA primary: `bg-accent-blue text-white px-8 py-3 rounded-lg font-medium`
- CTA secondary: `border border-default text-secondary px-8 py-3 rounded-lg font-medium hover:text-primary`
- npm install: `font-mono text-sm text-muted bg-surface-1 px-4 py-2 rounded border border-default` with copy button
- "Get Started Free" → links to `/signup` (or `https://app.sidclaw.com/signup`)
- "View on GitHub" → links to `https://github.com/sidclawhq/sdk`

#### Section 2: Problem

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│   Your agents are acting without oversight                       │
│                                                                  │
│   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │
│   │     73%     │  │     79%     │  │     37%     │            │
│   │ of CISOs    │  │ have blind  │  │ already had │            │
│   │ fear agent  │  │ spots in    │  │ agent-caused│            │
│   │ risks       │  │ agent ops   │  │ incidents   │            │
│   │ Only 30%    │  │             │  │             │            │
│   │ are ready   │  │             │  │             │            │
│   └─────────────┘  └─────────────┘  └─────────────┘            │
│                                                                  │
│   ¹ NeuralTrust State of AI Agent Security 2026                  │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

- Stats: `text-4xl font-bold text-primary`
- Descriptions: `text-sm text-secondary`
- Source citation: `text-xs text-muted`

#### Section 3: Four Primitives

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│   How it works                                                   │
│                                                                  │
│   Identity → Policy → Approval → Trace                          │
│                                                                  │
│   ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐   │
│   │ Identity │   │  Policy  │   │ Approval │   │  Trace   │   │
│   │          │   │          │   │          │   │          │   │
│   │ Every    │   │ Every    │   │ High-risk│   │ Every    │   │
│   │ agent is │   │ action   │   │ actions  │   │ decision │   │
│   │ governed │   │ evaluated│   │ get human│   │ creates  │   │
│   │ with an  │   │ against  │   │ review   │   │ auditable│   │
│   │ owner &  │   │ explicit │   │ with rich│   │ trace    │   │
│   │ scoped   │   │ rules    │   │ context  │   │          │   │
│   │ perms    │   │          │   │          │   │          │   │
│   └──────────┘   └──────────┘   └──────────┘   └──────────┘   │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

- Cards: `bg-surface-1 border-default rounded-lg p-6`
- Arrow connectors between cards (CSS borders or SVG)
- "Approval" card highlighted with `border-accent-amber` (it's the differentiator)

#### Section 4: Approval Demo

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│   See exactly what your agent wants to do — and decide           │
│                                                                  │
│   ┌──────────────────────────────────────────────────────────┐   │
│   │                                                          │   │
│   │  [Screenshot of approval detail panel from dashboard]    │   │
│   │                                                          │   │
│   └──────────────────────────────────────────────────────────┘   │
│                                                                  │
│   Every high-risk action surfaces a context-rich approval card.  │
│   One click to approve or deny.                                  │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

- Use a real screenshot from `research/screenshots/` (the approval detail panel)
- Or create a styled mock that looks like the real UI
- Image: `rounded-lg border border-default shadow-2xl`

#### Section 5: Comparison

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│   What makes this different                                      │
│                                                                  │
│   │ Capability        │ Traditional IAM │ SidClaw              │ │
│   │───────────────────│─────────────────│──────────────────────│ │
│   │ Agent Identity    │      ✓          │        ✓             │ │
│   │ Policy Evaluation │      ✓          │        ✓             │ │
│   │ Audit Trail       │      ✓          │        ✓             │ │
│   │ Approval Workflow │      —          │        ✓  ← the gap │ │
│   │ Context Cards     │      —          │        ✓             │ │
│   │ Risk Classification│     —          │        ✓             │ │
│   │ Integrity Hashes  │      —          │        ✓             │ │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

- Table: `bg-surface-1 rounded-lg`
- SidClaw column: checkmarks in `text-accent-green`
- "← the gap" annotation in `text-accent-amber`

#### Section 6: Use Cases

```
Three cards:
1. Finance (FINRA 2026) — "Pre-approval of AI use cases, human-in-the-loop, audit trails"
2. Healthcare (HIPAA) — "Scoped agent access to PHI, minimum necessary standard"
3. Platform Teams — "Govern agents at scale across your organization"
```

#### Section 7: Pricing

```
┌─ Free ────────────┐  ┌─ Team ────────────┐  ┌─ Enterprise ──────┐
│                    │  │                    │  │                    │
│  $0/month          │  │  Contact us        │  │  Custom            │
│                    │  │                    │  │                    │
│  5 agents          │  │  50 agents         │  │  Unlimited agents  │
│  10 policies/agent │  │  Unlimited policies│  │  Unlimited         │
│  2 API keys        │  │  10 API keys       │  │  Unlimited         │
│  7-day trace       │  │  90-day retention  │  │  Custom retention  │
│  retention         │  │                    │  │  SSO/OIDC          │
│  Community support │  │  Email support     │  │  Dedicated support │
│                    │  │                    │  │  SLA               │
│  [Get Started]     │  │  [Contact Sales]   │  │  [Contact Sales]   │
└────────────────────┘  └────────────────────┘  └────────────────────┘
```

- Cards: `bg-surface-1 border-default rounded-lg p-6`
- Enterprise card: `border-accent-blue` highlight

#### Section 8: Open Source

```
The SDK is Apache 2.0 open source.
Inspect every line. Self-host if you want.
We monetize the hosted platform, not the developer tool.

[View on GitHub]
```

#### Section 9: CTA Footer

```
Get started in 2 minutes

[Create Free Account]

or: npm install @sidclaw/sdk
```

### 3. Navigation Header

Sticky top nav:
- Logo/brand: "SidClaw" in `text-sm font-semibold`
- Links: Docs, GitHub, Pricing (scroll to section)
- CTA: [Sign In] [Get Started Free]
- `bg-surface-0/80 backdrop-blur-sm border-b border-default`

### 4. Footer

```
SidClaw · The approval layer for agentic AI
Docs · GitHub · Status
© 2026 SidClaw
```

### 5. SEO & Meta

```tsx
export const metadata = {
  title: 'SidClaw — The Approval Layer for Agentic AI',
  description: 'Identity, policy, approval, and audit for AI agents. Open-source SDK with enterprise-grade governance.',
  openGraph: {
    title: 'SidClaw — The Approval Layer for Agentic AI',
    description: 'Governance for AI agents. Human oversight for high-risk actions.',
    url: 'https://sidclaw.com',
    siteName: 'SidClaw',
    type: 'website',
  },
};
```

### 6. Package.json

```json
{
  "scripts": {
    "dev": "next dev --port 3002",
    "build": "next build",
    "start": "next start --port 3002"
  }
}
```

Port 3002 to avoid conflict with dashboard (3000) and docs (3001).

## Acceptance Criteria

- [ ] Landing page loads at `localhost:3002` with all 9 sections
- [ ] "Get Started Free" links to signup page
- [ ] "View on GitHub" links to `https://github.com/sidclawhq/sdk`
- [ ] npm install command has copy button
- [ ] All 3 market stats cite sources
- [ ] Pricing reflects actual free plan limits
- [ ] Four Primitives section highlights Approval as the differentiator
- [ ] Approval demo section has a real or realistic screenshot
- [ ] Dark theme: `#0A0A0B` background, `#E4E4E7` text
- [ ] No gradients, no AI sparkle, no decorative blobs
- [ ] Inter font for body, JetBrains Mono for code
- [ ] Responsive: works on mobile (single column)
- [ ] Lighthouse accessibility score >90
- [ ] Page loads in <2s (LCP)
- [ ] `turbo build` succeeds

## Constraints

- Do NOT implement any backend logic — this is a static marketing page
- Do NOT embed the dashboard — just link to it
- Follow code style: files in `kebab-case.tsx`, components in `PascalCase`

# Task: P5.5 — npm Publish & GitHub Repository Setup

## Context

You are working on the **Agent Identity & Approval Layer** project (brand: **SidClaw**, GitHub org: `sidclawhq`, npm org: `@sidclaw`). Read these files first:

1. `research/2026-03-20-product-development-plan.md` — read **P5.5** section.
2. `packages/sdk/package.json` — SDK package config (from P3.5).
3. `packages/sdk/README.md` — SDK README.
4. `.github/workflows/ci.yml` — existing CI workflow (from P0.6).

P3.5 made the SDK publish-ready. Your job is to set up the GitHub repository structure and CI/CD for publishing to npm.

**IMPORTANT:** This task sets up the TOOLING for publishing but does NOT actually publish. The actual `npm publish` will be done manually or via a tagged release after final review.

## What To Do

### 1. Update SDK Package for npm

Verify `packages/sdk/package.json` has:

```json
{
  "name": "@sidclaw/sdk",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/sidclawhq/sdk"
  },
  "homepage": "https://docs.sidclaw.com",
  "bugs": {
    "url": "https://github.com/sidclawhq/sdk/issues"
  }
}
```

### 2. GitHub Repository Files

Create these files at the project root (they'll be present in the repo that gets published):

#### `CONTRIBUTING.md`

```markdown
# Contributing to SidClaw SDK

Thank you for your interest in contributing! This document provides guidelines.

## Development Setup

1. Clone the repo: `git clone https://github.com/sidclawhq/sdk.git`
2. Install dependencies: `npm install`
3. Start the development stack: `docker compose up db -d && cd apps/api && npm run dev`
4. Run tests: `turbo test`

## Making Changes

1. Fork the repository
2. Create a feature branch: `git checkout -b feature/my-feature`
3. Make your changes
4. Run tests: `turbo test`
5. Run lint: `turbo lint`
6. Commit with a descriptive message
7. Push and create a Pull Request

## Code Style

- TypeScript with strict mode
- Files: `kebab-case.ts`
- Classes/interfaces: `PascalCase`
- Functions/variables: `camelCase`
- No `any` in public API surfaces

## SDK Changes

The SDK (`packages/sdk/`) is the open-source component (Apache 2.0).
Changes to the SDK should:
- Include unit tests
- Not break existing public API (semver)
- Update README if adding new exports
- Include JSDoc on public functions

## Reporting Issues

Use [GitHub Issues](https://github.com/sidclawhq/sdk/issues) with the provided templates.

## License

By contributing, you agree that your contributions will be licensed under the Apache 2.0 License.
```

#### `CODE_OF_CONDUCT.md`

Use the standard [Contributor Covenant v2.1](https://www.contributor-covenant.org/version/2/1/code_of_conduct/).

Contact email: `hello@sidclaw.com`

#### `.github/ISSUE_TEMPLATE/bug_report.md`

```markdown
---
name: Bug Report
about: Report a bug in the SidClaw SDK
title: '[Bug] '
labels: bug
---

## Description
A clear description of the bug.

## Steps to Reproduce
1. ...
2. ...
3. ...

## Expected Behavior
What should happen.

## Actual Behavior
What actually happens.

## Environment
- SDK version:
- Node.js version:
- Framework (if applicable):
- OS:

## Additional Context
Any additional information, error messages, or screenshots.
```

#### `.github/ISSUE_TEMPLATE/feature_request.md`

```markdown
---
name: Feature Request
about: Suggest a feature for the SidClaw SDK
title: '[Feature] '
labels: enhancement
---

## Problem
What problem does this solve?

## Proposed Solution
How should it work?

## Alternatives Considered
Any other approaches you've considered.

## Additional Context
Any additional information.
```

#### `.github/PULL_REQUEST_TEMPLATE.md`

```markdown
## Summary
What does this PR do?

## Changes
-

## Testing
- [ ] Unit tests added/updated
- [ ] Integration tests added/updated (if applicable)
- [ ] All existing tests pass (`turbo test`)

## Checklist
- [ ] Code follows the project's style guidelines
- [ ] No `any` types in public API
- [ ] README updated (if adding new exports)
- [ ] CHANGELOG updated (if user-facing change)
```

### 3. Release Workflow

Create `.github/workflows/release.yml`:

```yaml
name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write      # for creating GitHub Release
      id-token: write      # for npm provenance
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          registry-url: 'https://registry.npmjs.org'

      - run: npm ci

      - name: Run tests
        run: npx turbo test --filter=@sidclaw/sdk

      - name: Build SDK
        run: npx turbo build --filter=@sidclaw/sdk

      - name: Verify package
        working-directory: packages/sdk
        run: |
          npm pack --dry-run
          # Verify bundle size
          du -sh dist/

      - name: Publish to npm
        working-directory: packages/sdk
        run: npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: packages/sdk/*.tgz
```

### 4. Update CI Workflow

Update `.github/workflows/ci.yml` to also verify the SDK builds and packages correctly:

```yaml
  verify-sdk-package:
    runs-on: ubuntu-latest
    needs: [lint-and-typecheck, test-shared, test-api]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - name: Build SDK
        run: npx turbo build --filter=@sidclaw/sdk
      - name: Verify package contents
        working-directory: packages/sdk
        run: |
          npm pack --dry-run
          # Verify dist exists and has expected files
          test -f dist/index.js
          test -f dist/index.cjs
          test -f dist/index.d.ts
          test -f dist/mcp/index.js
          test -f dist/webhooks/index.js
          test -f README.md
          test -f LICENSE
      - name: Verify bundle size
        working-directory: packages/sdk
        run: |
          SIZE=$(du -sb dist/ | cut -f1)
          echo "Bundle size: $SIZE bytes"
          if [ $SIZE -gt 200000 ]; then
            echo "ERROR: Bundle size exceeds 200KB"
            exit 1
          fi
```

### 5. README Badges

Update `packages/sdk/README.md` to include badges at the top:

```markdown
# @sidclaw/sdk

[![npm version](https://img.shields.io/npm/v/@sidclaw/sdk)](https://www.npmjs.com/package/@sidclaw/sdk)
[![License: Apache-2.0](https://img.shields.io/badge/License-Apache%202.0-blue.svg)](https://opensource.org/licenses/Apache-2.0)
[![CI](https://github.com/sidclawhq/sdk/actions/workflows/ci.yml/badge.svg)](https://github.com/sidclawhq/sdk/actions)

Governance for AI agents. Identity, policy, approval, and audit.

[Documentation](https://docs.sidclaw.com) · [Quick Start](https://docs.sidclaw.com/docs/quickstart) · [Examples](https://github.com/sidclawhq/sdk/tree/main/examples)
```

### 6. Verify Everything

```bash
# Build SDK
cd packages/sdk && npm run build

# Verify pack contents
npm pack --dry-run

# Expected output should show ONLY:
# - package.json
# - README.md
# - LICENSE
# - CHANGELOG.md
# - dist/**

# Verify no secrets or internal URLs
grep -r "localhost" dist/ || echo "No localhost references in dist (good)"
grep -r "password" dist/ || echo "No password references in dist (good)"

# Check bundle size
du -sh dist/

# Verify npm audit
npm audit
```

## Acceptance Criteria

- [ ] CONTRIBUTING.md exists with development setup and contribution guidelines
- [ ] CODE_OF_CONDUCT.md exists (Contributor Covenant)
- [ ] Issue templates exist: bug report, feature request
- [ ] Pull request template exists
- [ ] Release workflow: tag push → test → build → publish to npm → GitHub Release
- [ ] CI workflow includes SDK package verification step
- [ ] README has badges (npm version, license, CI status)
- [ ] `npm pack --dry-run` shows only dist/, README, LICENSE, CHANGELOG, package.json
- [ ] No localhost URLs, passwords, or internal references in dist/
- [ ] Bundle size <200KB
- [ ] `npm audit` shows no vulnerabilities
- [ ] `turbo build` succeeds

## Constraints

- Do NOT actually publish to npm (that's a manual step after final review)
- Do NOT create the GitHub repository (the org exists, repo creation is manual)
- Do NOT modify SDK functionality
- Follow code style: kebab-case for files

# Google ADK × SidClaw Governance

Minimal Google Agent Development Kit example wrapping a tool with
`sidclaw.middleware.google_adk.govern_google_adk_tool`.

## Setup

```bash
cp .env.example .env
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
python agent.py
```

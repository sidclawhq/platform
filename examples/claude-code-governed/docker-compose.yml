services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: sidclaw_demo
      POSTGRES_USER: sidclaw
      POSTGRES_PASSWORD: sidclaw
    ports:
      - "5434:5432"
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
